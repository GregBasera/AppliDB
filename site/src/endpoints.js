var baseurl = "http://localhost:1337";

const Auth = baseurl + "/auth/local";
const Register = baseurl + "/auth/local/register";
const Applicants = baseurl + "/applicants";

export { Auth, Register, Applicants };
